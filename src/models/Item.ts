export default   class Item {
    id: string;
    activityName: string;
    timeTarget: number;

    constructor (
    id: string,
    activityName: string,
    timeTarget: number){
        this.id = id;
        this.activityName = activityName;
        this.timeTarget = timeTarget
    }

    // editItem
}